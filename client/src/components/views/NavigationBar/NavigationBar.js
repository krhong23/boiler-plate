import React, {Component} from 'react';

class NavBar extends Component {
    render() {
        return (
            <div>
                Navigation Bar
            </div>
        );
    }
}

NavBar.propTypes = {};

export default NavBar;